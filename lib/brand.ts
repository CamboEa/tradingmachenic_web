/** Public product name (display). */
export const BRAND_NAME = "FIN HUB KH";

/** Production domain — set `NEXT_PUBLIC_SITE_URL` to `https://${BRAND_DOMAIN}` when live. */
export const BRAND_DOMAIN = "finhubkh.com";
